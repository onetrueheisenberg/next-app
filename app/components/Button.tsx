"use client";
import React from "react";

const Button = () => {
  return (
    <button className="btn btn-primary" onClick={() => console.log("clkick")}>
      Click
    </button>
  );
};

export default Button;
